# =========================
# Custom additions for NBA Simulator Project
# =========================

# C++ / CMake / CUDA Build artifacts
CMakeCache.txt
CMakeFiles/
Makefile
cmake_install.cmake
compile_commands.json
*.o
*.obj
*.out
*.exe
*.dll
*.ptx
*.cubin
build/
bin/

# Docker & Database local volumes
# (Preventing large data being uploaded to GitHub)
postgres_data/
*.dump
*.bak

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python virtual environments (regenerate via requirements.txt)
.venv/
.venv-*/
venv/
__pycache__/
*.pyc

# Run logs (this project generates many during training/backtesting runs)
*.log

# Editor/tooling-local config, not part of the project itself
.vscode/
.claude/

# Before/after comparison snapshots kept locally during calibration work --
# not canonical; see README.md / ml_model/hybrid_pipeline_benchmark.json's
# own prior_configurations for the historical numbers these represented.
*.prev
*.prev_*
